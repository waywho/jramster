$('#stateform select').hide();
$('#state-status').show();
$('#icon-span').append("<%= j image_tag state_icon(@page.workflow_state), :class => 'state-icon' %>");
$('#current-state').html('<%= j @page.current_state.to_s %>');
var currentState = $('#current-state').text();
$('#update-date').html('<%= j @page.updated_at.to_s %>');
buttonChange();

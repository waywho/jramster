<div class = 'col-xs-offset-2 col-xs-8'>
	<table>
	<% @photos.each do |photo| %>
	<div>
		<%= image_tag photo.picture, :class => 'img-responsive' %>
		<br />
		<%= photo.caption %>
	</div>
	<% end %>
</div>
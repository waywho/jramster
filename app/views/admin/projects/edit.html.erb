<div class = 'col-xs-offset-2 col-xs-8'>
	<%= simple_form_for @project, :url => admin_project_path do |f| %>
	<%= f.input :title %>
	<%= f.input :overview, input_html: {:id => 'eg-basic', :class => 'text-small'} %>
	<%= f.input :details, input_html: {:id => 'eg-basic', :class => 'text-small'} %>
	<%= f.button :submit %>
	<% end %>

	<br />

	<%= simple_form_for @photo, :url => admin_project_photos_path(@photo) do |f| %>
	<%= f.input :picture %>
	<%= f.input :description %>
	<%= f.check_box :feature_img %>
	<%= f.button :submit %>
	<% end %>
<div>


<script type="text/javascript">
  $(document).ready(function(){

	$(function() {
	    $('#eg-basic').editable({
	      inlineMode: false
	    })
  	});

  })
</script>
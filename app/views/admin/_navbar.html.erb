    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to admin_pages_path, :class => 'navbar-brand' do %>
          JRamster CMS
          <% end %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
            <li <%= 'class = active' if params[:controller] == 'admin/pages' %> ><%= link_to 'Pages', admin_pages_path %><span class="sr-only">(current)</span></li>
            <li <%= 'class = active' if params[:controller] == 'admin/projects' %> ><%= link_to 'Projects', admin_projects_path %><span class="sr-only">(current)</span></li>
            <li <%= 'class = active' if params[:controller] == 'admin/diaries' %> ><%= link_to 'Diaries', admin_diaries_path %><span class="sr-only">(current)</span></li>
            <li <%= 'class = active' if params[:controller] == 'admin/photos' %> ><%= link_to 'Photos', admin_photos_path %><span class="sr-only">(current)</span></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if current_user %>
            <li><%= link_to current_user.email, '#s' %></li>
            <li><%= link_to 'Sign Out', destroy_user_session_path %></li>
            <% else %>
            <li><%= link_to 'Sign In', new_user_session_path %></li>
            <% end %>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>
